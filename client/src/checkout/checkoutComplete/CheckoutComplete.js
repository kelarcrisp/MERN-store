import React from "react";
import classes from "./CheckoutComplete.module.css";
const CheckoutComplete = () => {
  return (
    <div className={classes.CheckoutCompleteContainer}>
      Your order is on it's way!
    </div>
  );
};

export default CheckoutComplete;
